h4. Normal Workflow Screencast

The second screencast we've provided is _Normal Workflow in Git_, which demonstrates how to setup your @.gitignore@ file, how to use and interpret @git status@ output, how to add and remove files from your index, how to commit and what git does in the object database during these operations.

movie. c2-normal-workflow.mov